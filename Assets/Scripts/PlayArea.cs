using System.Collections;
using System.Collections.Generic;
using ChainBreakers;
using UnityEngine;

public class PlayArea : MonoBehaviour
{
    Card card;
    Sprite playedCard;



    // Update is called once per frame
    void Update()
    {
        //get the local position of the card, if the card is within the play area, make a small sprite of the card into the playList
    }
}
